#include <stdlib.h>
#include "main.h"
/**
 * _abs - computes the absolute value of integer
 * @i: The number to computer
 *
 * Return: return the absolute value of this integer
 */
int _abs(int n)
{
	return (abs(n));
}
